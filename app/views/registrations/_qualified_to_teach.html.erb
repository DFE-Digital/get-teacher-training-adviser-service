<%= f.govuk_collection_select :qualified_subject,
  ApiClient::get_teaching_subjects,
  :id,
  :value,
  label: { text: "Which subject are you qualified to teach?", tag: "h1", size: "m" }
%>
<%= f.govuk_submit %>