<%= f.govuk_radio_buttons_fieldset(:primary_or_secondary, legend: { text: 'Which stage are you interested in teaching?'} ) do %>
  <%= f.govuk_radio_button :primary_or_secondary, 'primary', label: { text: 'Primary' } %>
  <%= f.govuk_radio_button :primary_or_secondary, 'primary with maths', label: { text: 'Primary with maths' } %>
  <%= f.govuk_radio_button :primary_or_secondary, 'secondary', label: { text: 'Secondary' } %>
<% end %>
<%= f.govuk_submit%>